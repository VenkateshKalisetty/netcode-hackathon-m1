{
  "name": "wd_m1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:dev": "nodemon --ignore 'front-end/' server.js",
    "start": "npm install && cd front-end && npm install && npm run build && cd .. && node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "~4.16.1",
    "jsonwebtoken": "^8.5.1",
    "sequelize": "^6.3.3",
    "sqlite3": "^5.0.0"
  }
}
